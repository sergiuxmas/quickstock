spring:
  profiles:
    active: local
  datasource:
    url: jdbc:postgresql://localhost:5434/quickstock_payments
    username: quickstock
    password: secret
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate

spring:
  datasource:
    username: quickstock
    password: secret
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate
security:
  jwt:
    secret: "CHANGE_ME_CHANGE_ME_CHANGE_ME_CHANGE_ME_32+CHARS"
    issuer: "quickstock-payments"

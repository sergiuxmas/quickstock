spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/quickstock_core
    username: quickstock
    password: secret
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate

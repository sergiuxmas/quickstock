# Contracts Impact

This feature is governance/process-only and does not change runtime APIs or callback payloads.

## Impact Assessment

- Core service REST contracts: no change
- Payments service REST contracts: no change
- Core <-> Payments callback contracts: no change
- Service-owned OpenAPI files under `quickstock-core-service/src/main/openapi/` and `payments-service/src/main/openapi/`: no updates required for this feature

## Rationale

The scope of `001-spec-kit-guidance-scope` is specification workflow boundaries, versioning governance, and planning readiness checks.

